{
  "name": "rpg-assistant-prototype",
  "version": "0.5.0",
  "private": true,
  "type": "module",
  "description": "Local assistant for online text role-playing sessions",
  "scripts": {
    "start": "node server.mjs",
    "test": "node server.mjs --self-test",
    "check": "node --check server.mjs && node --check public/app.js"
  },
  "engines": {
    "node": ">=20"
  }
}
